// Faça um programa em C que mostre a tabuada do número 5. (considerar tabuada do número 1 ao 10). 

#include <stdio.h>

int main () {
  int i, mult;

  while (i <= 10) {
    mult = i * 5
      Printf("5 * i = %d", mult);
  }

  return 0;
}