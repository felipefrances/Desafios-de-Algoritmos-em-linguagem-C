// Desenvolva um algoritmo que calcule a soma de todos os números ímpares
// múltiplos de três no intervalo de 1 a 500.

#include <stdio.h>

int main() {

  int n1, n, an, r, soma;
  r = 2;
  n = 500;
  an = 499;

  soma = n / 2 * (a1 + an);
  

  printf("A soma dos números ímpares de 1 a 500 é: %d\n", soma);

  return 0;
}